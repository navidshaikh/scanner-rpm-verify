scanner-rpm-verify
==================


Atomic Scanner for verifying installed RPM packages.

To run:

```
$ sudo atomic install registry.centos.org/pipeline-images/scanner-rpm-verify:latest
$ sudo atomic scan --scanner rpm-verify <image_name>
```
